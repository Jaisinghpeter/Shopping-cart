{{outlet}}
<div class="container">
    <div class="row">
        <div class="col-sm-4">
            {{#each model as |product|}}
                {{product.name}}
            {{/each}}
        </div>
    </div>
</div>